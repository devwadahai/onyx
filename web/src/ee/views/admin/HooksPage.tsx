// Original stub — NOT derived from onyx-dot-app/onyx's actual ee/ source
// (never read; separately licensed — see ../../../../../../basalt/NOTICE.md).
// FR-ENT-04 (extension hooks) isn't built on Basalt's backend — see
// docs/basalt-implementation-status.md — so this page is a placeholder,
// not a functional gap silently papered over.
export default function HooksPage() {
  return <p>Extension hooks admin UI — not implemented on this backend.</p>
}
